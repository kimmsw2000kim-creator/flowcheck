package com.board.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // Configuration beans (e.g. WebClient, model mapper) will be defined here.
}
